using Gtk 4.0;
using Adw 1;

template MillionaireWindow: Adw.ApplicationWindow {
    default-width: 800;
    default-height: 500;

    Box {
        orientation: vertical;

        HeaderBar header_bar {}
    }
}
